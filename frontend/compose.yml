version: "3.8"

services:
  dev:
    build:
      context: .
      dockerfile: ./dockerfile
    environment:
      CHOKIDAR_USEPOLLING: "true"
    env_file:
      - .env
    ports:
      - "5173:5173"
